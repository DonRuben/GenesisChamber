# === REQUIRED ===
OPENROUTER_API_KEY=your-openrouter-api-key

# === OPTIONAL (media generation) ===
FAL_KEY=your-fal-key-here
ELEVENLABS_API_KEY=

# === DATABASE (Vercel Postgres / Neon — optional) ===
# When set, enables persistent storage that survives Render.com redeploys.
# Without it, the system falls back to file-based JSON storage (local dev).
# Get this from: Vercel Dashboard > Storage > Postgres > .env.local tab
# DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# === DEPLOYMENT (only needed for production) ===
# PORT=8001
# ALLOWED_ORIGINS=https://your-frontend.vercel.app,http://localhost:5173
